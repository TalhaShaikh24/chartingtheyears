# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
config.bat

# Uploaded cover images (stored on the server's disk, not in git).
# Anchored to the root so src/app/api/uploads/ is NOT ignored.
/uploads/

# Personal Claude Code preferences
CLAUDE.local.md
